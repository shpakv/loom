---
description: Loom phase 3 — build the walking skeleton: thinnest end-to-end slice, ADRs proposed in the moment
---
Use the `loom-skeleton-phase` skill (read `loom-core` first).

Verify inputs: approved GLOSSARY, VISION (minimum), and exactly one approved
first-slice epic. Capture every one-way fork as a proposed ADR immediately;
give every new package its SKILL.md at birth. Do not accept one-way ADRs —
that is /loom:consolidate's job.
