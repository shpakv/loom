---
description: Loom design phase — decompose the approved epic into design doc, tracks, and session-sized tasks
argument-hint: [epic-<slug>]
---
Use the `loom-design-phase` skill (read `loom-core` first) on epic: $ARGUMENTS

Contracts first, tracks as conflict-free workstreams, tasks sized to one
session. Leave `## Plan` sections empty — plans are made just-in-time in
/loom:implement. Finish with review of design.md.
