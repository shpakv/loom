---
description: Scaffold Loom project state (docs skeleton, config, gate scripts) into the current repository
argument-hint: [--refresh]
---
Use the `loom-init` skill (read `loom-core` first) with arguments: $ARGUMENTS
